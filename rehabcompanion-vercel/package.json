{
  "name": "rehabcompanion",
  "version": "1.0.0",
  "description": "SaaS platform for rehabilitation clinics - Vercel deployment & Local development",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:client\"",
    "dev:api": "cd api && npm run dev:watch",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd api && npm install && cd ../client && npm install",
    "build": "cd client && npm run build"
  },
  "keywords": [
    "healthtech",
    "rehabilitation",
    "gamification"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}