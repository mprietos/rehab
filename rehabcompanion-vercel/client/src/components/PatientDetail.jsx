import React from 'react';
// This component is now integrated into DoctorDashboard.jsx as a Dialog
// Keeping this file as a placeholder if we want to split it out later
const PatientDetail = () => {
    return <div>Integrated in DoctorDashboard</div>;
};
export default PatientDetail;
